<ui:FluentWindow x:Class="CL_CLegendary_Launcher_.Windows.PixiColorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
        Title="CL - ColorPicker" Height="520" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="None"
        Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ColorPicker;component/Styles/DefaultColorPickerStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="Transparent" />
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="Transparent" />

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{DynamicResource MainForegroundBrush}" />
            </Style>
            <Style TargetType="{x:Type Label}">
                <Setter Property="Foreground" Value="{DynamicResource MainForegroundBrush}" />
            </Style>

            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="{DynamicResource MainBackgroundButton}" />
                <Setter Property="Foreground" Value="{DynamicResource MainForegroundBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#33FFFFFF" />
                <Setter Property="Padding" Value="4,0" />
                <Setter Property="Height" Value="20" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="4">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type RadioButton}">
                <Setter Property="Foreground" Value="{DynamicResource MainForegroundBrush}" />
                <Setter Property="Margin" Value="2,0" />
            </Style>

            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Visibility" Value="Collapsed" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{DynamicResource MainBackgroundBrush}" 
            CornerRadius="16" BorderThickness="1" BorderBrush="#33FFFFFF">

        <Grid Margin="20,0,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" 
                    MouseLeftButtonDown="Window_MouseLeftButtonDown" 
                    Cursor="SizeAll">
                <TextBlock Text="⋮" FontWeight="Bold" FontSize="16" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Foreground="{DynamicResource MainForegroundBrush}" Opacity="0.3" />
            </Border>
            
            <colorpicker:StandardColorPicker x:Name="MyColorPicker" 
                                             Grid.Row="1"
                                             Background="{DynamicResource MainBackgroundBrush}"
                                             Foreground="{DynamicResource MainForegroundBrush}" 
                                             ShowAlpha="False" />

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="BtnCancel" Content="Скасувати" Width="100" Height="35" Margin="0,0,10,0" 
                        Background="Transparent" Foreground="{DynamicResource MainForegroundBrush}" 
                        BorderThickness="1" BorderBrush="#33FFFFFF" Click="BtnCancel_Click" Cursor="Hand" FontFamily="Inter 18pt 18pt"/>

                <Button x:Name="BtnApply" Content="Застосувати" Width="100" Height="35" 
                        Background="{DynamicResource MainBackgroundButton}" Foreground="{DynamicResource MainForegroundBrush}" BorderThickness="0" 
                        Click="BtnApply_Click" Cursor="Hand" FontFamily="Inter 18pt 18pt">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</ui:FluentWindow>